name: Metrics

on:
  # 毎日 00:00 JST に再生成
  schedule:
    - cron: "0 15 * * *"
  # Actions タブから手動実行する用（初回生成はこれを使うと早い）
  workflow_dispatch:

permissions:
  contents: write

jobs:
  github-metrics:
    # metrics は内部で Docker イメージを実行するため、Docker 非対応の
    # ubuntu-slim は使えない。フル VM の ubuntu-latest を使う。
    runs-on: ubuntu-latest
    steps:
      - name: Generate metrics
        uses: lowlighter/metrics@latest
        with:
          # 基本機能は GITHUB_TOKEN で動作する。
          # private contributions や一部プラグインを使いたい場合は、
          # PAT を作成して repo secret `METRICS_TOKEN` に登録し、下行を
          # `token: ${{ secrets.METRICS_TOKEN }}` に差し替える。
          token: ${{ secrets.GITHUB_TOKEN }}
          user: ik11235
          filename: github-metrics.svg
          template: classic
          config_timezone: Asia/Tokyo

          # --- ステータス本体（旧 Stats Card 相当）---
          base: header, activity, community, repositories, metadata

          # --- 使用言語の内訳 ---
          plugin_languages: yes
          plugin_languages_details: bytes-size, percentage

          # NOTE: plugin_achievements は GitHub 側の実績取得が不安定で
          # 「Unexpected error」表示になる既知問題(issue #1479)のため無効化。
